require 'spec_helper'

describe Quoting::PinneyQuoter::Results::Result do
  # it { should have_one(:approved_case) }
  # it { should have_one(:quoted_case) }
  # it { should have_one(:submitted_case) }
  # it { should belong_to(:user) }
  # it { should belong_to(:carrier) }
  # it { should belong_to(:policy_type) }
  # it { should belong_to(:quote_param_dyn) }
  # it { should belong_to(:connection) }
  # 
  # it "should respond for all attributes" do
  #   quoting_result = FactoryGirl.build(:quoting_pinney_quoter_results_result)
  #   [ "annual_premium", "carrier_id", "carrier_health_class", 
  #     "plan_name", "planned_modal_premium", "policy_type_id", 
  #     "connection_id", "user_id", "quote_param_dyn_id", 'monthly_premium'
  #   ].each do |attr|
  #     quoting_result.should respond_to(attr)
  #   end
  # end
end
