<%= form_for @website, url: '/website/', :remote => true do |f| %>

  <%= f.hidden_field :contact_info_id, :value => @contact_info.id %>

  <div class="row">
    <div class="span2"><%= f.label :url %></div>
    <div class="span10">
      <%= f.text_field :url, {:style => "width:300px"} %>
    </div>
  </div>
  <div class="row">
    <div class="span3"></div>
    <div class="span3">
      <button type="button" id="submit<%= get_contact_info_for %>-website-form" class="btn btn-primary ajax">
        Add
      </button>
    </div>
  </div>
<% end %>
